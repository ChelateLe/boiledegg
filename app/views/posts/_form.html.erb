<%= form_with(model: @post, local: true) do |f|%>
  <%= f.label :image, "投稿する絵を選択してね" %>
  <%= f.file_field :image %>

  <%= f.label :title, "タイトル" %>
  <%= f.text_field :title %>

  <%= f.label :text, "絵の説明、コメント" %>
  <%= f.text_area :text %>

  <%= f.submit "投稿する！" %>
<% end %>